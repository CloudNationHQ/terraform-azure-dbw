# Storage firewall

This example restricts access to the default storage account, using an access connector with a user assigned identity.

Azure only supports this configuration on vnet injected workspaces, therefore the workspace is deployed into a virtual network.
